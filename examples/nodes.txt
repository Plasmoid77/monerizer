# Кандидаты Monero-нод для `monerizer node list` / `node select` (NODE-01).
# Формат: host rpc_port zmq_port (hostname или IPv4; IPv6-литералы не поддерживаются). Строки с # — комментарии.
# Источник: публичный список monero.fail, отфильтрованный 2026-09-11 по
# get_info (synchronized) и открытому ZMQ-порту. Открытый порт не гарантирует
# стабильный ZMQ — окончательная проверка: `monerizer logs p2pool` без
# повторяющихся «ZMQReader disconnected». Список сопровождает владелец файла.
# Удалённая нода видит IP хоста и адрес выплат (NODE-05); локальная нода приватнее.
xmr.support 18081 18083
hantaan.fullm00n.de 18089 18083
xmr.jacoblightning3.xyz 18081 18083
xmr.privacy.cash 18081 18083
194.163.176.218 18089 18083
